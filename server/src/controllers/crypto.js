// import { randomBytes } from 'crypto';
// import * as fs from 'fs';

// // Genera una chiave segreta casuale con lunghezza 32 byte (256 bit)
// const generateSecretKey = () => {
//   return randomBytes(32).toString('hex');
// };

// // Ottieni la chiave segreta
// const secretKey = generateSecretKey();

// // Scrivi la chiave segreta nel file .env
// fs.writeFileSync('.env', `JWT_SECRET=${secretKey}\n`, { flag: 'a' });

// console.log('Chiave segreta generata e aggiunta al file .env.');

// export default secretKey;
